PyQt6>=6.4.0
Pillow>=10.0.0
imagehash>=4.3.1
exifread>=3.0.0
numpy>=1.24.0
scipy>=1.10.0

# Face recognition (requires cmake + Visual Studio build tools on Windows)
# Install separately after base install:
#   pip install cmake
#   pip install dlib
#   pip install face-recognition
